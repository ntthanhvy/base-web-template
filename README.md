# Base SPA Web app using bun + React + Vite

After download copy .env.example to .env (or .env.local) modifiled file content according to running environment.

```bash
# install
bun install

# run
bun run dev
```



